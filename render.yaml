services:
  - type: web
    name: blackeye-complete
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: EMAIL_TO
        value: felixkoskey278@gmail.com
      - key: EMAIL_FROM
        value: felixkoskey278@gmail.com
      - key: EMAIL_APP_PASSWORD
        value: ntsu adxv tfgw ptpj
